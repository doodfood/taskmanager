When points are awarded, theres less points the later a task is completed. However on the main page where it displays the tasks with the complete button, it always shows maximum points even when a task is overdue. It should show the amount of points you will get if you complete the task today.

When someone has only 1 completed overdue task for the week, they get back on track which is correct, they are also on track to get eager bunny streak which is not correct as they had an overdue task in that week so the eager bunny streak is broken as is the amazing worker streak. I think the issue is when the task is due sunday and the badges get awarded monday, the task technically never went overdue during the week so it doesn't get counted as an overdue task. I think we need to handle this edge case specifically, when a job is due on sunday and the badge ceremony is on monday we run the badge calculation as at sunday I presume? When running the badge ceremony awards we need to look for jobs that are due on the sunday but not yet completed and count those as overdue jobs for the purpose of the badges, otherwise they roll over in to the next day and the user gets awarded all the streak badges when they didn't complete their jobs. Are there other edge cases like this? I'm not sure, what if a job starts on sat and ends on monday? or starts on sat and ends on tuesday? 

